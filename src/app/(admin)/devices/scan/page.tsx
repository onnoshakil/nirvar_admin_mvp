"use client";

import { Typography } from "antd";

const { Title } = Typography;

export default function ScanDevicePage() {
  return (
    <div>
      <Title level={3}>Scan Device QR Code</Title>
      {/* TODO: Shakil — build QRScanner component here */}
    </div>
  );
}
