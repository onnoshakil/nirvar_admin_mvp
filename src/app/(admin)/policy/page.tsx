"use client";

import { Typography } from "antd";

const { Title } = Typography;

export default function PolicyPage() {
  return (
    <div>
      <Title level={3}>Global Policy</Title>
      {/* TODO: Shakil — build PolicyEditor component here */}
    </div>
  );
}
