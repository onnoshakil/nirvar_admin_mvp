"use client";

import { Typography } from "antd";

const { Title } = Typography;

export default function StudentsPage() {
  return (
    <div>
      <Title level={3}>Students</Title>
      {/* TODO: Shakil — build StudentTable component here */}
    </div>
  );
}
