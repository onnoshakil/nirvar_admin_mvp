"use client";

import { Typography } from "antd";

const { Title } = Typography;

export default function InstitutionDetailPage() {
  return (
    <div>
      <Title level={3}>Institution Detail</Title>
      {/* TODO: Nahid — build institution detail with tabs here */}
    </div>
  );
}
